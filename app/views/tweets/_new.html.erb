<%if user_signed_in? %>
       <div class="card text-center">
              <div class="card-body">
                     <%= form_with(model: @tweet, local: true) do |f| %>
                            <%= f.text_area :content, size: '60x5', placeholder: "Twitea algo!" %><br>
                            <%= f.submit "Subir" %>
                     <%end%>
              </div>
       </div>
<%else%>
       <h3 class="text-center">Si deseas Twiter debes ingresar o crearte una cuenta</h3>
<%end%>